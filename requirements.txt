# Omani Mental Health Chatbot - Voice-Enabled Requirements
streamlit>=1.28.0
python-dotenv>=1.0.0
requests>=2.31.0

# LangChain packages (newer versions for better Python 3.13 compatibility)
langchain>=0.2.0
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-core>=0.2.0

# Direct API packages (fallback support)
openai>=1.0.0
anthropic>=0.7.0

# Azure Speech Services for Voice Interface
azure-cognitiveservices-speech>=1.34.0
azure-identity>=1.15.0

# Audio processing and WebSocket support
soundfile>=0.12.1
numpy>=1.24.0
websockets>=11.0
asyncio-throttle>=1.0.2

# Additional Streamlit components for voice
streamlit-webrtc>=0.47.0
streamlit-mic-recorder>=0.0.4

# Dual-Model Validation System Dependencies
dataclasses-json>=0.6.0  # For structured data handling
pydantic>=2.0.0  # For data validation
asyncio-mqtt>=0.16.0  # For async operations 
streamlit-webrtc
streamlit-mic-recorder